// import { AuthType } from './auth.types';

const initialState = {};

// interface authReducerIF {
//   data: Object;
//   type: AuthType;
// }

// const authReducer = (state = initialState, action: authReducerIF) => state;
const authReducer = (state = initialState) => state;

export default authReducer;
